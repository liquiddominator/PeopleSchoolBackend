spring:
  datasource:
    url: jdbc:h2:mem:people_school_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/people_school_test
    redis:
      host: localhost
      port: 6379
